{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "run-java-contestant",
            "type": "shell",
            "options": {
                "cwd": "${workspaceFolder}"
            },
            "command": "./run_java_contestant.sh"
        }
    ]
}