{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "run-python-contestant",
            "type": "shell",
            "options": {
                "cwd": "${workspaceFolder}"
            },
            "command": "./run_python_contestant.sh"
        }
    ]
}
